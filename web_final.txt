practice no 1)
<!DOCTYPE html>
<html>
<head>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; margin: 0; background: #fdfcfb; color: #444; }
        header { text-align: center; padding: 2rem; border-bottom: 2px solid #eee; }
        .flex { display: flex; align-items: center; gap: 2rem; padding: 2rem; max-width: 900px; margin: auto; }
        .p-img { width: 150px; height: 150px; border-radius: 50%; background: #eee; }
        .grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; padding: 0 2rem; max-width: 900px; margin: auto; }
        .box { padding: 1.5rem; border-radius: 10px; }
        footer { text-align: center; padding: 2rem; background: #f7f7f7; margin-top: 2rem; }
        a { color: #555; text-decoration: none; font-weight: 600; margin: 0 10px; }
    </style>
</head>
<body>

<header>
    <h1>Vinay motwani</h1>
    <p>Designation: Intelligent Collaborator</p>
</header>

<div class="flex">
    <img src="https://via.placeholder.com/150" class="p-img">
    <div>
        <h2>My Desire</h2>
        <p>To assist humans in creative problem solving through seamless, intuitive interactions.</p>
    </div>
</div>

<div class="grid">
    <div class="box" style="background: #e3f2fd;"><h3>Hobbies</h3>Reading data, Coding</div>
    <div class="box" style="background: #f1f8e9;"><h3>Passion</h3>Knowledge sharing</div>
    <div class="box" style="background: #fff3e0;"><h3>Past Work</h3>Language modeling</div>
    <div class="box" style="background: #f3e5f5;"><h3>Social Life</h3>Global connectivity</div>
</div>

<footer>
    <a href="#">Social</a> | <a href="#">LinkedIn</a>
</footer>

</body>
</html>
practice no 2 
<!DOCTYPE html>
<html>
<head>
    <title>Traffic Light</title>

    <style>
        body {
            text-align: center;
            font-family: Arial;
            background-color: #f5f5f5;
        }

        .light {
            width: 100px;
            height: 100px;
            margin: 10px auto;
            border-radius: 50%;
            background-color: grey;
        }

        .red { background-color: red; }
        .green { background-color: green; }
        .yellow { background-color: yellow; }

        button {
            padding: 10px 20px;
            margin-top: 20px;
            font-size: 16px;
        }
    </style>
</head>

<body>

    <h1>Traffic Light</h1>

    <div id="red" class="light"></div>
    <div id="green" class="light"></div>
    <div id="yellow" class="light"></div>

    <button onclick="changeLight()">Next</button> <!-- Inline Event -->

    <script>
        let currentLight = "red";

        function changeLight() {

            // Turn all lights OFF
            document.getElementById("red").className = "light";
            document.getElementById("green").className = "light";
            document.getElementById("yellow").className = "light";

            // Decide next light using if-else
            if (currentLight === "red") {
                currentLight = "green";
            } else if (currentLight === "green") {
                currentLight = "yellow";
            } else {
                currentLight = "red";
            }

            // Turn ON selected light
            document.getElementById(currentLight).classList.add(currentLight);
        }
    </script>

</body>
</html>
practice no 3)
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio + DOM Practical</title>

    <!-- External CSS -->
    <link rel="stylesheet" href="styles.css">

    <!-- Internal CSS -->
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f6ff;
            transition: 0.3s;
        }

        header {
            background: #dcd6f7;
            padding: 20px;
            text-align: center;
        }

        .container {
            display: flex;
            padding: 30px;
        }

        .profile-img {
            width: 200px;
            border-radius: 10px;
        }

        .intro {
            margin-left: 30px;
        }

        .section {
            margin: 20px;
            padding: 20px;
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        button {
            margin: 5px;
            padding: 8px 15px;
            cursor: pointer;
        }

        footer {
            background: #dcd6f7;
            text-align: center;
            padding: 15px;
        }
    </style>
</head>
<body>

<header>
    <h1>Vinay Motwani</h1>
    <p>Web Developer | Engineer</p>
</header>
Practice no 5
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>React Controlled Form</title>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
        body { font-family: sans-serif; padding: 20px; background: #f4f7f6; }
        .container { background: white; padding: 20px; border-radius: 8px; max-width: 400px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        input { width: 100%; padding: 8px; margin: 10px 0; box-sizing: border-box; }
        button { background: #5c67f2; color: white; border: none; padding: 10px 15px; cursor: pointer; border-radius: 4px; }
        .error { color: red; font-size: 12px; margin-top: -8px; }
        .output { margin-top: 20px; padding: 10px; background: #e8f5e9; border-left: 5px solid #4caf50; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState } = React;

        function FormComponent() {
            // 1. State for form fields
            const [user, setUser] = useState({ name: '', email: '' });
            // State for validation errors
            const [errors, setErrors] = useState({});
            // State for displaying submitted data
            const [displayData, setDisplayData] = useState(null);

            // 2. Event Handler for input changes
            const handleChange = (e) => {
                const { name, value } = e.target;
                setUser({ ...user, [name]: value });
                setErrors({ ...errors, [name]: '' }); // Clear error when typing
            };

            // 3. Validation and Submission
            const handleSubmit = (e) => {
                e.preventDefault();
                let tempErrors = {};
                
                if (!user.name) tempErrors.name = "Name is required";
                if (!/\S+@\S+\.\S+/.test(user.email)) tempErrors.email = "Invalid email format";

                if (Object.keys(tempErrors).length === 0) {
                    setDisplayData(user); // Process and display data
                    setErrors({});
                } else {
                    setErrors(tempErrors);
                    setDisplayData(null);
                }
            };

            return (
                <div className="container">
                    <h2>User Form</h2>
                    <form onSubmit={handleSubmit}>
                        <input 
                            name="name" 
                            placeholder="Full Name" 
                            value={user.name} 
                            onChange={handleChange} 
                        />
                        {errors.name && <p className="error">{errors.name}</p>}

                        <input 
                            name="email" 
                            placeholder="Email Address" 
                            value={user.email} 
                            onChange={handleChange} 
                        />
                        {errors.email && <p className="error">{errors.email}</p>}

                        <button type="submit">Submit</button>
                    </form>

                    {/* 4. Display submitted data dynamically */}
                    {displayData && (
                        <div className="output">
                            <h3>Submitted Data</h3>
                            <p><strong>Name:</strong> {displayData.name}</p>
                            <p><strong>Email:</strong> {displayData.email}</p>
                        </div>
                    )}
                </div>
            );
        }

        // Render the component
        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<FormComponent />);
    </script>
</body>
</html>
<div class="container">
    <img src="profile.jpg" class="profile-img" style="border:3px solid #a393eb;">
    <div class="intro">
        <h2>About Me</h2>
        <p id="aboutText">I am passionate about web development.</p>
        <button onclick="changeText()">Change Text</button>
    </div>
</div>

<!-- Dynamic List Section -->
<div class="section">
    <h2>My Tasks</h2>
    <ul id="list"></ul>

    <button onclick="addItem()">Add Item</button>
    <button onclick="removeItem()">Remove Last Item</button>
</div>

<!-- Background Color Section -->
<div class="section">
    <h2>Change Background Color</h2>

    <button onclick="changeColor('lightblue')">Blue</button>
    <button onclick="changeColor('lightgreen')">Green</button>
    <button onclick="changeColor('lightpink')">Pink</button>
</div>

<footer>
    <p>Connect with me:</p>
    <a href="#">LinkedIn</a>
    <a href="#">GitHub</a>
</footer>

<script>

// 1. Change paragraph text
function changeText() {
    document.getElementById("aboutText").innerText = "Text changed using JavaScript DOM!";
}

// 2. Add list item
let count = 1;
function addItem() {
    let li = document.createElement("li");
    li.innerText = "Task " + count++;
    document.getElementById("list").appendChild(li);
}

// 3. Remove last item
function removeItem() {
    let list = document.getElementById("list");
    if (list.lastChild) {
        list.removeChild(list.lastChild);
        count--;
    }
}

// 4. Change background color
function changeColor(color) {
    document.body.style.backgroundColor = color;
}

</script>

</body>
</html>

/* External CSS (styles.css) */
body {
    line-height: 1.6;
}

h2 {
    color: #5a4fcf;
}
practice no 6
npm install -g @angular/cli
ng new my-angular-app
cd my-angular-app
ng g s item
ng serve

